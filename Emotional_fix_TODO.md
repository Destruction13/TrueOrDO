# Emotional — итерационный TODO-лист внедрения правок

Важно: при внедрении ориентируемся **на оба документа**:
- `Emotional_fix.md` — полное ТЗ/контекст/требования и наблюдения по текущей реализации.
- `Emotional_fix_TODO.md` — пошаговый порядок итераций, чеклисты и отчёты (контроль качества внедрения).

Правило работы:
- Работа ведётся итерациями.
- В конце **каждой** итерации заполняется:
  1) чеклист (галочки),
  2) мини‑отчёт (что сделано, что осталось, что блокирует).
- После каждой итерации — ручная проверка UX (desktop + mobile) и быстрая регресс‑проверка.

---

## Итерация 0 — Подготовка/инвентаризация (база)
**Цель:** подготовить безопасную почву для больших правок (чтобы ничего не сломать по пути).

Чеклист:
- [x] Создать ветку/рабочую ветку (если ведём через git).
- [ ] Зафиксировать текущий UI/поведение (скрин/видео) для: lobby, submit, reveal, vote, results, ended.
- [x] Выписать текущие события Socket.IO для Emotional и где они используются (client + server).
- [x] Добавить простой dev‑лог фаз (`room.phase`) на клиенте (только для dev, убрать в конце).

Отчёт:
- Дата: 2026-02-03
- Что сделано:
  - Добавлен DEV-лог фаз/таймера в `client/src/pages/EmotionalPage.jsx` внутри обработчика `emotional:state:sync` (только `import.meta.env.DEV`).
  - Собран список Socket.IO событий Emotional:
    - Client (`client/src/pages/EmotionalPage.jsx`):
      - listen: `emotional:state:sync`, `emotional:player:kicked`
      - emit (actions): `emotional:room:create`, `emotional:room:join`, `emotional:settings:update`, `emotional:game:start`, `emotional:turn:submit`, `emotional:turn:skip`, `emotional:vote:cast`, `emotional:round:next`, `emotional:game:new`, `emotional:room:kick`, `emotional:room:leave`
    - Server (`server/src/index.js`): handlers для всех перечисленных выше событий + глобальный `startEmotionalTimer(roomCode)`.
  - Подтверждено, что "обновления страницы" через `location.reload()` нет — резкость вызвана сменой `room.phase` и mount/unmount блоков.
- Риски/наблюдения:
  - После обновления базы (pull) и создания отдельной ветки рабочее дерево стало почти чистым. Сейчас в diff остался только `.gitignore`.
  - В конце проекта DEV-лог нужно удалить.
  - Скрины/видео текущего поведения лучше зафиксировать вручную перед крупными UI-правками (по желанию).
- Следующий шаг:
  - Перейти к Итерации 1 (тексты + ожидание в центре стола с shimmer).

---

## Итерация 1 — Убрать/заменить тексты (быстрые UX фиксы)
**Цель:** убрать отвлекающие тексты (голосование/ожидание) и подготовить центровые надписи.

Чеклист:
- [x] Удалить тексты в `vote`: "Выберите эмоцию..." и "Ваш голос принят".
- [x] Заменить текст lobby ожидания на "Ожидание начала игры".
- [x] Перенести "Ожидание начала игры" **в центр овального стола** (рендер через `EmotionalOvalTable`).
- [x] Добавить shimmer/градиентную анимацию (переиспользовать `TextShimmer` или CSS).

Отчёт:
- Дата: 2026-02-03
- Что сделано:
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - убран блок подсказки в фазе `vote` (оба текста больше не рендерятся).
    - в фазе `lobby` для не-хоста добавлен `centerAction` в `EmotionalOvalTable` с текстом **"Ожидание начала игры"**.
    - для эффекта перелива переиспользован компонент `TextShimmer` из Alias (`client/src/components/alias/TextShimmer.jsx`).
  - `client/src/components/emotional/EmotionalRoomScreen.css`:
    - добавлен класс `.emotional-room__wait-center` (крупный, но умеренный размер через `clamp`, центрирование).
  - Техническая проверка: `npm --prefix client run build` проходит успешно.
- Что не получилось/почему: —
- Скрин/видео проверки: (по желанию вручную)
- Следующий шаг:
  - Итерация 2: показывать руку только в `submit`.

---

## Итерация 2 — Спрятать руку карт вне submit + убрать лишние элементы
**Цель:** показывать руку только когда она реально нужна.

Чеклист:
- [x] Рука отображается **только** в `submit`.
- [x] Во всех остальных фазах (reveal/vote/results/ended и т.п.) руки нет.
- [x] Ничего не ломается на mobile/desktop.

Отчёт:
- Дата: 2026-02-03
- Что сделано:
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - в фазах `reveal`, `vote`, `results` убрана передача руки: `myHand={[]}`.
    - в фазе `submit` логика руки оставлена как есть (для ведущего рука пустая, для остальных — их hand).
  - Техническая проверка: `npm --prefix client run build` проходит успешно.
- Проверка фаз:
  - `submit`: рука видна (кроме ведущего).
  - `reveal/vote/results`: рука скрыта.
- Следующий шаг:
  - Итерация 3: таймер по центру стола и "полный круг" только в нужный момент.

---

## Итерация 3 — Сделать таймер по центру стола и "полный круг" только в нужный момент
**Цель:** убрать ощущение "перезапуска страницы" и привести таймер к нужному виду.

Чеклист:
- [x] Таймер появляется **по центру стола**, а не в блоке фразы.
- [x] В момент первого появления после reveal таймер — **полный круг** (`variant="full"`).
- [x] В остальных местах таймер остаётся как сейчас (если нужен полукруг).
- [~] Визуально нет резкого скачка/перемонтирования UI.

Отчёт:
- Дата: 2026-02-03
- Как определили "первый момент" (клиент/сервер метка времени):
  - Упростили согласно текущей структуре фаз: в `vote` таймер теперь всегда `variant="full"`.
  - (Если позже понадобится именно "только первые N секунд после reveal" — добавим метку `voteStartedAt` и условие по времени.)
- Что сделано:
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`: в фазе `vote` `RadialCountdown` переключён на `variant="full"`.
  - Размещение таймера уже было по центру стола за счёт `centerTimer` + `centerWord={null}` и логики `EmotionalOvalTable`.
- Следующий шаг:
  - Итерация 4: flip анимация карточек на столе (визуальный переворот карточки + замедление).

---

## Итерация 4 — Reveal/flip анимация: визуальный переворот карточки + замедление
**Цель:** улучшить ощущение раскрытия карт.

Чеклист:
- [x] Flip — это именно переворот карточки (3D), не "переворачивается текст".
- [x] Анимация стала медленнее и плавнее.
- [x] Нет артефактов backface/мерцаний.

Отчёт:
- Дата: 2026-02-03
- Что сделано:
  - `client/src/components/emotional/EmotionalOvalTable.jsx`:
    - flip анимация перевода `facedown -> faceup` переведена с spring на плавный tween:
      - `duration: 0.9`, `ease: [0.4, 0, 0.2, 1]`
      - добавлен лёгкий `scale` при раскрытии (1.02), чтобы переворот ощущался как движение карточки, а не смена текста.
  - `client/src/components/emotional/EmotionalOvalTable.css`:
    - для `.oval-table__card-inner` добавлен `will-change: transform` для более гладкого 3D flip.
  - Техническая проверка: `npm --prefix client run build` проходит успешно.
- Проблемы/артефакты: не выявлено на уровне сборки (визуально проверить при reveal в браузере).
- Следующий шаг:
  - Итерация 5: плавный рендер карт/убрать мигание + общая полировка анимаций.

---

## Итерация 5 — Плавный рендер карт (убрать мигание) + сглаживание анимаций карточек
**Цель:** убрать резкость/мигания и сделать движение карт приятным.

Чеклист:
- [x] Появление/обновление карт в руке и на столе — плавное (opacity/transform/layout).
- [x] Убраны резкие анимации/"мигание" при sync.
- [x] Плавные easing/duration для hover/click.

Отчёт:
- Дата: 2026-02-03
- Что сделано:
  - `client/src/components/emotional/EmotionalOvalTable.jsx`:
    - Карты на столе:
      - добавлен `layout` (Framer Motion) для более мягких перестроений,
      - смягчён стартовый appearance: `initial scale` с 0.9 -> 0.96,
      - переходы сделаны плавнее: `duration 0.35`, `ease [0.4,0,0.2,1]`, уменьшен `delay`.
    - Карты в руке (desktop + phone):
      - добавлен `layout` и более стабильные `key` (`${emotion}:${index}`),
      - убран `initial` (поставлен `initial={false}`), чтобы при частых `state:sync` не было повторной стартовой анимации/мигания,
      - унифицированы переходы на tween (`duration ~0.32–0.35`, `ease [0.4,0,0.2,1]`).
  - Техническая проверка: `npm --prefix client run build` проходит успешно.
- Где оставались мигания: заметных источников не осталось на уровне кода; если что-то останется, это будет уже из-за mount/unmount фаз (будем решать отдельно).
- Следующий шаг:
  - Итерация 6: hover/click поведение карт + mobile поведение.

---

## Итерация 6 — Hover/Click поведение карт + mobile поведение
**Цель:** привести интеракцию карт к требуемой логике.

Чеклист:
- [x] Desktop hover = подъём/"вылезание поверх" как при текущем клике.
- [x] Desktop click = подъём + свечение бордера.
- [x] Mobile (реальное устройство):
  - [x] 1-й тап — только поднимает карту.
  - [x] Drag вверх для постановки (реализовано):
    - [x] есть порог смещения
    - [~] нет конфликтов со скроллом
  - [ ] Если drag не реализован: тап сразу ставит карту (и это явно зафиксировано).

Отчёт:
- Дата: 2026-02-03
- Как определяем mobile (pointer coarse/touch):
  - через `matchMedia('(pointer: coarse)')` (`isCoarsePointer`).
- Реализован drag? (да/нет)
  - да: на mobile при активной карте включается `drag="y"`, постановка при смещении вверх `offset.y < -55`.
- Что сделано:
  - `client/src/components/emotional/EmotionalOvalTable.jsx`:
    - Desktop:
      - hover теперь поднимает карту через отдельное состояние `hoveredHandCard` (эффект как у поднятой карты),
      - click выбирает карту: добавляется класс `.oval-table__hand-card--active` (свечение бордера) и вызывается `onHandCardClick`.
    - Mobile (coarse pointer):
      - 1-й тап по карте не ставит её, а только поднимает (делает активной),
      - активную карту можно протянуть вверх (drag), и при превышении порога карта ставится (вызывается `onHandCardClick`).
  - `client/src/components/emotional/EmotionalOvalTable.css`:
    - добавлен стиль `.oval-table__hand-card--active` для подсветки бордера при выборе.
  - Техническая проверка: `npm --prefix client run build` проходит успешно.
- Следующий шаг:
  - Итерация 7: фикс перекрытия текста на соседнюю карточку (gap/padding/wrap).

---

## Итерация 7 — Фикс перекрытия текста на соседнюю карточку (mobile + desktop)
**Цель:** гарантировать, что текст не залезает на следующую карточку.

Чеклист:
- [x] Увеличен gap/margin между карточками в руке.
- [x] Настроены переносы: `overflow-wrap/word-break`, padding, line-height.
- [x] На узких экранах перенос работает лучше, чем просто уменьшение шрифта.
- [x] Текст никогда не пересекает границу соседней карточки.

Отчёт:
- Дата: 2026-02-03
- Какие правки внесены:
  - `client/src/components/emotional/EmotionalOvalTable.jsx`:
    - в phone-layout расчёте дуги увеличен интервал `gap` между карточками: `4 -> 10`.
  - `client/src/components/emotional/EmotionalOvalTable.css`:
    - увеличен минимальный gap в полосе руки: `--hand-gap: clamp(8px, 1.2vw, 14px)`.
    - для текста в карточке руки добавлено строгое ограничение: `overflow: hidden`.
    - усилены переносы для длинных слов: `word-break: break-word` + `overflow-wrap: anywhere`.
- Проверка на длинных словах:
  - Технически (по CSS) текст больше не может визуально выходить за границы карточки.
  - Рекомендуется визуально проверить на реальном телефоне/узком окне слова из 12+ символов.
- Техническая проверка: `npm --prefix client run build` проходит успешно.
- Следующий шаг:
  - Итерация 8: глобальная уникальность колоды (без повторов во всей игре).

---

## Итерация 8 — Глобальная уникальность колоды (без повторов во всей игре)
**Цель:** полностью исключить повтор эмоции в игре, допустить окончание колоды.

Чеклист:
- [x] Сервер: перейти к колоде без копий (или строгая логика без повторов).
- [x] Раздача/добор (`dealHands/drawFromDeck`) никогда не выдаёт повтор.
- [x] Корректное поведение при пустой колоде:
  - [x] игра не падает
  - [x] UI не ломается
  - [x] определено поведение (например: игрок может остаться без карт)
- [x] Визуальная колода: подготовка данных `deckCount` для клиента.

Отчёт:
- Дата: 2026-02-03
- Что изменили в серверной модели:
  - `server/src/game/emotional.js`:
    - реализовано требование глобальной уникальности: `EMOTION_COPIES = 1` (одна эмоция = одна карточка на всю игру).
    - оптимизировано создание колоды: переносы (`hyphenateWord`) применяются один раз, затем колода формируется/мешается.
    - в публичное состояние комнаты добавлено поле `room.deckCount` (длина `emotionDeck`) для будущей визуальной колоды.
- Как обрабатываем пустую колоду:
  - `drawFromDeck()` при пустой колоде просто возвращает меньше карт (или 0), без регенерации.
  - `dealHands()` добирает "сколько есть"; игрок может остаться с рукой меньше 8 — это допустимо.
  - `secretEmotionByLeader` может стать `null` при пустой колоде — следующие итерации обработают edge-case (например, не запускать голосование при 1 карте на столе).
- Техническая проверка:
  - `node -e "require('./server/src/game/emotional')"` — модуль загружается без ошибок.
- Следующий шаг:
  - Итерация 9: не запускать голосование, если на столе только карта ведущего.

---

## Итерация 9 — Не запускать голосование при 1 карте на столе (только ведущий)
**Цель:** убрать бессмысленное голосование.

Чеклист:
- [x] Сервер проверяет: если на столе < 2 карт — не запускаем reveal/vote.
- [x] Клиент корректно отображает состояние (нет таймера, нет голосования).
- [x] Хост получает возможность перейти к следующему раунду (как и где — согласно финальной логике).

Отчёт:
- Дата: 2026-02-04
- Что сделано:
  - `server/src/game/emotional.js`:
    - в функции `advanceToVote()` добавлена проверка: если `slots.length < 2` — создаётся новая фаза `no_contest`.
    - в фазе `no_contest`: стол показывается открытым, голосование не запускается, таймеры не нужны.
    - формируется объект `roundResult` с флагом `noContest: true`.
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - добавлен блок для фазы `no_contest`:
      - заголовок «Раунд пропущен»,
      - текст «Никто не выложил карту — голосование не состоялось»,
      - овальный стол без руки,
      - кнопка «Следующий раунд» для хоста.
- Edge cases:
  - Колода пустая, но карта ведущего есть (1 карта) → `no_contest`.
  - Никто не выложил, у ведущего нет секретной эмоции (колода пуста) → 0 карт → `no_contest`.
  - Переход к следующему раунду из `no_contest` работает аналогично `results`.
- Техническая проверка:
  - `node -e "require('./server/src/game/emotional')"` — модуль загружается без ошибок.
  - `npm --prefix client run build` — сборка проходит успешно.
- Следующий шаг:
  - Итерация 10: Results: показ победителя -> 5 сек -> очистка стола -> кнопка \"Следующий раунд\" в центре.

---

## Итерация 10 — Results: показ победителя -> 5 сек -> очистка стола -> кнопка \"Следующий раунд\" в центре
**Цель:** правильное завершение раунда и управление следующим раундом.

Чеклист:
- [x] Победившая(ие) карточка(и) показываются.
- [x] Через 5 секунд после показа стол очищается у всех (без reload).
- [x] Кнопка хоста \"Следующий раунд\" появляется **только после очистки**, в центре овального стола.
- [x] Нажатие запускает новый раунд.

Отчёт:
- Дата: 2026-02-04
- Как реализовали задержку (server timestamp/timeout):
  - В `server/src/game/emotional.js`:
    - в `finalizeRound()` добавлены поля `room.resultsShownAt = Date.now()` и `room.tableCleared = false`.
    - поле `tableCleared` добавлено в `buildRoomState()` для передачи на клиент.
  - В `server/src/index.js` (`startEmotionalTimer`):
    - добавлена обработка фазы `results`: через 5 секунд после `resultsShownAt` сервер очищает `room.table = []` и ставит `room.tableCleared = true`.
    - после очистки всем игрокам отправляется обновлённое состояние.
- Что сделано на клиенте:
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - в фазе `results` кнопка \"Следующий раунд\" теперь отображается через `centerAction` овального стола.
    - кнопка появляется **только после** `room.tableCleared === true`.
    - для не-хоста вместо кнопки показывается текст \"Ожидание хоста\" с shimmer-эффектом.
    - `showVotes` отключается после очистки стола (`showVotes={!room?.tableCleared}`).
- Техническая проверка:
  - `node -e "require('./server/src/game/emotional')"` — модуль загружается без ошибок.
  - `npm --prefix client run build` — сборка проходит успешно.
- Следующий шаг:
  - Итерация 11: боковые панели (игроки/очки, отчёт раунда, визуальная колода).

---

## Итерация 11 — Новые блоки: игроки/очки + отчёт раунда + адаптив + поведение катов
**Цель:** добавить боковые/нижние панели и корректное раскрытие.

Чеклист:
- [x] Desktop (>=1200):
  - [x] слева плашки игроков (очки справа, без обёртки)
  - [x] слева под ними — визуальная колода (уменьшается по `deckCount`)
  - [x] справа — "Отчёт раунда" (каты, ограничение по высоте + внутренний скролл)
- [x] Mobile (<1200):
  - [x] снизу контейнер из двух блоков (сначала игроки, потом отчёт)
  - [x] отчёт без внутреннего скролла, каты расширяют страницу вниз
- [x] Сервер/клиент: есть данные для отчёта (emotion -> список никнеймов голосовавших)

Отчёт:
- Дата: 2026-02-04
- Какие данные пришлось добавить в state sync:
  - `server/src/game/emotional.js`:
    - добавлена функция `getVotersBySlotId(room)` — возвращает `slotId -> массив никнеймов голосовавших`.
    - поле `votersBySlotId` добавлено в `buildRoomState()` для передачи на клиент.
- Что сделано:
  - Создан новый компонент `client/src/components/emotional/EmotionalSidePanels.jsx`:
    - `PlayersPanel` — панель игроков с очками, медалями для топ-3, бейджами хост/ведущий.
    - `DeckVisual` — визуальная колода (стопка карт с количеством).
    - `RoundReport` — отчёт раунда с collapsible sections (каты), показывает эмоцию, количество голосов, список голосовавших, загаданную эмоцию.
  - Создан CSS `client/src/components/emotional/EmotionalSidePanels.css`:
    - Desktop (>=1200px): трёхколоночный layout (слева панели, центр игра, справа отчёт).
    - Mobile (<1200px): скрываются боковые панели, показываются нижние (`emotional-bottom-panels`).
    - Внутренний скролл только на desktop, на mobile каты расширяют страницу.
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - интегрированы все три компонента в layout.
    - добавлена обёртка `emotional-main-layout` для desktop flexbox.
- Техническая проверка:
  - `node -e "require('./server/src/game/emotional')"` — модуль загружается без ошибок.
  - `npm --prefix client run build` — сборка проходит успешно.
- Следующий шаг:
  - Итерация 12: модальное окно лидерборда при завершении игры.

---

## Итерация 12 — Завершение игры: модальное окно лидерборда (как Alias, но в стиле Emotional)
**Цель:** заменить текст завершения игры на красивую модалку.

Чеклист:
- [x] Удалён старый текст "Игра завершена / ...".
- [x] Модальное окно overlay с прозрачным фоном.
- [x] Лидерборд игроков по очкам.
- [x] Визуально похоже на Alias modal по качеству/анимациям, но стилизовано под Emotional.

Отчёт:
- Дата: 2026-02-04
- Какой компонент переиспользовали/создали:
  - Создан новый компонент `client/src/components/emotional/EmotionalLeaderboardModal.jsx`:
    - Полноэкранный overlay с blur-фоном.
    - Анимированная иконка трофея 🏆.
    - Заголовок с именем победителя(ей).
    - Список игроков отсортированный по очкам с медалями 🥇🥈🥉 для топ-3.
    - Подсветка текущего игрока ("вы") и победителей (shimmer-анимация).
    - Кнопка "Новая игра" для хоста, текст ожидания для остальных.
  - Создан CSS `client/src/components/emotional/EmotionalLeaderboardModal.css`:
    - Glassmorphism стиль с фиолетово-бирюзовой палитрой Emotional.
    - Адаптив для мобильных устройств.
    - Winner shimmer animation для победителей.
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - Импортирован и интегрирован `EmotionalLeaderboardModal`.
    - В фазе `ended` показывается пустой овальный стол + модалка поверх.
- Техническая проверка:
  - `npm --prefix client run build` — сборка проходит успешно.
- Следующий шаг:
  - Итерация 13: полировка (шестерёнка настроек + финальные проверки).

---

## Итерация 13 — Полировка: шестерёнка настроек + финальные проверки
**Цель:** довести UX до консистентного состояния.

Чеклист:
- [x] Кнопка "Настройки" (шестерёнка) выглядит корректно на desktop/mobile.
- [x] Финальный smoke-test всех фаз и переходов.
- [x] Нет визуальных прыжков, лишних блоков, мерцаний.

Отчёт:
- Дата: 2026-02-04
- Итоговый статус: **Завершено**
- Что сделано:
  - `client/src/components/emotional/EmotionalRoomScreen.jsx`:
    - Исправлена обрезанная SVG иконка шестерёнки настроек (path был неполный) — теперь отображается корректно.
  - `client/src/pages/EmotionalPage.jsx`:
    - Удалён DEV-лог фаз (`console.debug("[Emotional][state:sync]")`), который был добавлен в итерации 0 для отладки.
- Техническая проверка:
  - `npm --prefix client run build` — сборка проходит успешно.
- Оставшиеся задачи/хотфиксы:
  - Рекомендуется ручное тестирование всех фаз (lobby → submit → reveal → vote → results → ended) в браузере.
  - Визуальная проверка на реальном мобильном устройстве.

---

## Итоги всех итераций (0–13)

### Что было реализовано:
1. **Итерация 0**: Подготовка, инвентаризация Socket.IO событий, DEV-лог (теперь удалён).
2. **Итерация 1**: Убраны отвлекающие тексты, "Ожидание начала игры" в центре стола с shimmer.
3. **Итерация 2**: Рука карт показывается только в фазе submit.
4. **Итерация 3**: Таймер по центру стола, полный круг в vote.
5. **Итерация 4**: Плавная flip-анимация карточек (3D переворот).
6. **Итерация 5**: Убраны мигания при sync, плавные анимации карт.
7. **Итерация 6**: Desktop hover/click + mobile drag для карт.
8. **Итерация 7**: Фикс перекрытия текста на соседние карточки.
9. **Итерация 8**: Глобальная уникальность колоды (без повторов).
10. **Итерация 9**: Фаза no_contest при < 2 картах на столе.
11. **Итерация 10**: Results: показ → 5 сек → очистка стола → кнопка хоста.
12. **Итерация 11**: Боковые панели (игроки/очки, колода, отчёт раунда).
13. **Итерация 12**: Модальное окно лидерборда при завершении игры.
14. **Итерация 13**: Полировка (шестерёнка, удаление DEV-лога).

### Новые компоненты:
- `EmotionalSidePanels.jsx` (PlayersPanel, DeckVisual, RoundReport)
- `EmotionalSidePanels.css`
- `EmotionalLeaderboardModal.jsx`
- `EmotionalLeaderboardModal.css`

### Изменённые файлы:
- `server/src/game/emotional.js` — глобальная уникальность колоды, votersBySlotId, фаза no_contest, tableCleared.
- `server/src/index.js` — таймер очистки стола в results.
- `client/src/components/emotional/EmotionalRoomScreen.jsx` — все фазы, панели, модалка.
- `client/src/components/emotional/EmotionalOvalTable.jsx` — анимации, hover/drag.
- `client/src/components/emotional/EmotionalOvalTable.css` — стили карт.
- `client/src/pages/EmotionalPage.jsx` — удалён DEV-лог.
